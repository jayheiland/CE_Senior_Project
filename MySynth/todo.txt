To-do:

Sidebar functionality - load from file into dict, allow switching between presets using sidebar
Sliders in GUI
Make it so that load button is only enabled upon connecting to synth
Test if the serial error is a constant problem, or if it can be fixed by resetting the STM
uniform variable naming convention